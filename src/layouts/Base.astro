---
import config from "$config";

import BaseHead from "@components/Layouts/BaseHead.astro";

import "@fontsource/work-sans/variable.css";

interface Props {
  title?: string;
  description?: string;
}

const { title, description } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead
      title={title ? title : config.site.title}
      description={description ? description : config.site.description}
    />
  </head>
  <body
    class="p-4 prose prose-headings:text-lg prose-headings:font-medium bg-neutral-50 md:mx-auto"
  >
    <div class="mt-4 md:mt-16 lg:mt-24">
      <slot />
    </div>
  </body>
</html>
